# คู่มือการนำเกมขึ้นออนไลน์ (Deployment Guide) แบบละเอียดที่สุด

คู่มือนี้จะพาคุณครูทำทีละคลิก ตั้งแต่การเอาไฟล์ในคอมพิวเตอร์ขึ้นอินเทอร์เน็ต ไปจนถึงการเปิดให้ทุกคนเข้ามาเล่นได้ฟรีครับ

> [!IMPORTANT]
> **กฎเหล็กก่อนเริ่ม:** ห้ามอัปโหลดโฟลเดอร์ `node_modules`, `.next` และไฟล์ที่ลงท้ายด้วย `.db` เด็ดขาด เพราะจะทำให้ระบบรันไม่ผ่านครับ

---

## 🐙 ขั้นตอนที่ 1: นำไฟล์ขึ้น GitHub

GitHub เปรียบเสมือนโกดังเก็บโค้ดของเราครับ

1. เปิดเว็บ [GitHub.com](https://github.com/) และเข้าสู่ระบบให้เรียบร้อย
2. มองหาเครื่องหมายบวก **`+`** ที่มุมขวาบนสุดของจอภาพ แล้วคลิกเลือก **`New repository`**
3. ตรงช่อง **Repository name** ให้พิมพ์ชื่อโปรเจกต์ เช่น `my-democracy-game` (ห้ามเว้นวรรค ใช้ขีดกลางแทน)
4. เลื่อนลงมาด้านล่างสุด แล้วคลิกปุ่มสีเขียว **`Create repository`**
5. เมื่อเข้าสู่หน้าจอถัดไป ให้มองหาข้อความสีน้ำเงินเล็กๆ ที่เขียนว่า **`uploading an existing file`** (อยู่ตรงกลางๆ จอ) ให้คลิกตรงนั้นครับ
6. **ถึงเวลาลากไฟล์:** เปิดโฟลเดอร์เกมในเครื่องคอมพิวเตอร์ของคุณครู เลือกเฉพาะรายการต่อไปนี้ แล้วลากไปปล่อยในหน้าเว็บ GitHub:
   - 📁 โฟลเดอร์ `app`
   - 📁 โฟลเดอร์ `lib`
   - 📁 โฟลเดอร์ `public`
   - 📄 ไฟล์ `package.json`
   - 📄 ไฟล์ `package-lock.json`
   - 📄 ไฟล์ `next.config.mjs`
   - 📄 ไฟล์ `eslint.config.mjs`
   - 📄 ไฟล์ `jsconfig.json`
   - 📄 ไฟล์ `postcss.config.mjs`
   *(ไฟล์อื่นนอกจากนี้ ไม่ต้องลากไปครับ)*
7. รอจนแถบโหลดไฟล์ทุกอันเต็ม 100% จากนั้นเลื่อนลงมาล่างสุด แล้วกดปุ่มสีเขียว **`Commit changes`**
8. รอสักครู่ (อาจจะนานนิดนึง) เมื่อเสร็จแล้ว คุณครูจะเห็นไฟล์ทั้งหมดไปโชว์อยู่ในหน้า GitHub ครับ

---

## 🚀 ขั้นตอนที่ 2: ผูกโกดังโค้ดเข้ากับ Vercel

Vercel คือเซิร์ฟเวอร์ที่จะเอาโค้ดในโกดัง GitHub ของเราไปเปิดเป็นเว็บไซต์ครับ

1. เปิดเว็บ [Vercel.com](https://vercel.com/) และเข้าสู่ระบบ (เลือก Login with GitHub)
2. เมื่อเข้ามาหน้าหลัก ให้กดปุ่มสีดำ **`Add New...`** (มุมขวาบน) แล้วเลือก **`Project`**
3. คุณครูจะเห็นชื่อโกดัง `my-democracy-game` ที่เราเพิ่งสร้างใน GitHub โผล่ขึ้นมา ให้กดปุ่ม **`Import`** หลังชื่อนั้น
4. จะเข้ามาสู่หน้า Configure Project ไม่ต้องตั้งค่าอะไรเลยครับ เลื่อนลงไปกดปุ่มสีดำ **`Deploy`** ได้เลย
5. นั่งรอประมาณ 1-2 นาที... หน้าจอจะเปลี่ยนเป็นรูปพลุฉลอง 🎉 **(ยินดีด้วย เว็บไซต์ออนไลน์แล้ว!)**
6. ให้คลิกที่ปุ่ม **`Continue to Dashboard`** เพื่อเข้าไปตั้งค่าฐานข้อมูลต่อครับ

---

## 💾 ขั้นตอนที่ 3: สร้างฐานข้อมูล (Database)

ตอนนี้เว็บรันได้แล้ว แต่ยังเซฟข้อมูลกลุ่มและไพ่ไม่ได้ เราต้องสร้างกล่องเก็บข้อมูลให้มันครับ

1. ตอนนี้คุณครูอยู่ที่หน้า Dashboard ของ Vercel สังเกตเมนูแนวนอนด้านบน ให้คลิกที่คำว่า **`Storage`**
2. มองหาและคลิกปุ่ม **`Connect Database`**
3. เลือกแถบที่เขียนว่า **`Postgres`** (โลโก้ช้างสีฟ้า) แล้วกดปุ่ม `Continue`
4. ระบบจะขึ้นหน้าต่างให้ยอมรับเงื่อนไข (Accept) ให้กดปุ่มกดยืนยัน (Create) ไปเรื่อยๆ จนเสร็จขั้นตอน
5. เมื่อเสร็จแล้ว คุณครูจะเห็นว่ามีก้อนฐานข้อมูลชื่อ `Postgres` ปรากฏขึ้นมาในหน้า Storage เป็นอันเสร็จสิ้นครับ

---

## 🔄 ขั้นตอนที่ 4: รีสตาร์ทระบบเพื่อให้เชื่อมต่อกันสมบูรณ์ (Redeploy)

ขั้นตอนสุดท้าย เราต้องสั่งให้ Vercel รีสตาร์ทตัวเองเพื่อรับรู้ว่ามีฐานข้อมูลมาต่อแล้ว

> [!TIP]
> วิธีที่ง่ายและชัวร์ที่สุดในการสั่ง Vercel รีสตาร์ท คือการแกล้งไปแก้ไฟล์ใน GitHub ครับ

1. สลับหน้าต่างกลับไปที่เว็บ **GitHub.com** (โกดังของเรา)
2. ในหน้ารายชื่อไฟล์ ให้คลิกเข้าไปที่ไฟล์ที่ชื่อว่า **`README.md`**
3. มองหา **ไอคอนรูปดินสอ ✏️** (มุมขวาบนของกล่องเนื้อหาไฟล์) แล้วคลิกเพื่อแก้ไข
4. พิมพ์ข้อความอะไรก็ได้ต่อท้ายลงไป เช่น พิมพ์คำว่า `อัปเดตระบบ` หรือเว้นวรรค 1 ทีก็ได้ครับ
5. เลื่อนไปด้านขวาบน กดปุ่มสีเขียว **`Commit changes...`** และกดตกลง
6. ทันทีที่คุณครูกดตกลง Vercel จะแอบไปทำงานอยู่หลังบ้าน (รีสตาร์ทระบบทั้งหมดใหม่ อัตโนมัติ!)

**เสร็จสิ้นทุกขั้นตอน!** 🏆 
รอประมาณ 2 นาที แล้วคุณครูลองเข้าเว็บจากลิงก์ที่ Vercel ให้มาได้เลยครับ (ลิงก์จะหน้าตาประมาณ `my-democracy-game.vercel.app`) สามารถสร้างกลุ่มและเล่นได้แบบสมบูรณ์ 100% เลยครับ!
